language: rust
before_install:
  - sudo apt-get install -y libdbus-1-dev
os:
  - linux
  - osx
  - windows
rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
